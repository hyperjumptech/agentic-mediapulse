name: agentic-mediapulse
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - agent-framework
      - python-dotenv
      - httpx
      - psycopg[binary]
      - sqlmodel
      - fastapi
      - uvicorn
